# 인계 문서 — Claude Code에서 이어서 작업하기

이 저장소 하나로 세 가지가 됩니다.

1. **pip 설치 가능한 파이썬 라이브러리** (`src/jeongsan`)
2. **Claude Code 스킬** (`skills/jeongsan`)
3. **Claude Code 플러그인** (`.claude-plugin/plugin.json`)

셋 다 같은 룰 엔진을 씁니다. 판정 로직은 한 곳에만 있습니다.

---

## 30초 만에 시작하기

```bash
git clone <저장소> jeongsan && cd jeongsan
pip install -e ".[dev]"
pytest -q                 # 97 passed
claude                    # 이 디렉터리에서 실행 → 스킬 자동 인식
```

`.claude-plugin/` 이 저장소 루트에 있으므로, 이 폴더에서 Claude Code를 켜면
`skills/jeongsan` 이 바로 잡힙니다. 별도 설치가 필요 없습니다.

---

## 설치 방법 세 가지

어느 것을 고를지는 **누가 쓰느냐**로 정하십시오.

### A. 나 혼자, 모든 프로젝트에서 (개인 스킬)

```bash
mkdir -p ~/.claude/skills
cp -r skills/jeongsan ~/.claude/skills/
pip install -e /경로/jeongsan          # CLI를 PATH에 올림
```

가장 단순합니다. 다만 스킬 안에서 `${CLAUDE_SKILL_DIR}/../../src` 경로가
깨지므로 **pip 설치를 반드시 함께** 하십시오.

### B. 이 저장소에서 작업할 때만 (프로젝트 스킬)

아무것도 안 해도 됩니다. `skills/` 가 이미 저장소 안에 있고,
Claude Code는 `.claude/skills/` 와 플러그인 루트를 함께 봅니다.
저장소 루트에서 `claude` 를 실행하십시오.

다른 프로젝트에 얹고 싶다면:

```bash
mkdir -p /대상프로젝트/.claude/skills
ln -s /경로/jeongsan/skills/jeongsan /대상프로젝트/.claude/skills/jeongsan
```

### C. 여러 사람에게 배포 (플러그인 — 권장)

가장 정석입니다. `bin/jeongsan` 이 PATH에 자동으로 붙어서
**받는 사람이 pip 설치를 안 해도 됩니다.**

```bash
# 개발·테스트 중
claude --plugin-dir /경로/jeongsan

# git 저장소로 배포
/plugin marketplace add https://github.com/<계정>/jeongsan
/plugin install jeongsan@school-admin
```

`marketplace.json` 의 `owner` 와 저장소 URL만 실제 값으로 바꾸면 됩니다.
학교나 교육청 단위로 배포한다면 이 방식을 쓰십시오.

---

## 첫 대화에서 이렇게 확인하십시오

Claude Code를 켜고 그대로 붙여넣으면 됩니다.

```
jeongsan 스킬이 인식되는지 확인하고, tests/fixtures 의 "오류 다발" 시나리오를
돌려서 어떤 오류가 잡히는지 보여줘.
```

기대 결과 — 오류 6건이 나와야 합니다.

| 룰 | 내용 |
|---|---|
| R01 | 방음패널 `24 × 22,000 = 528,000` 인데 견적서엔 550,000 |
| R03 | 부가세 324,000이어야 하는데 310,000 |
| R05 | 기안 2,640,000 vs 견적 3,550,000 |
| R09 | 추정가격 2,400만원인데 1인 견적 |
| R12 | 견적 유효기간 39일 경과 |
| R14 | 사업자등록번호 체크섬 불일치 |

이게 그대로 나오면 인계 완료입니다.

---

## 지금 상태

**되는 것**

- 텍스트·엑셀 붙여넣기 수준의 기안·견적서·세금계산서 파싱
- 16개 룰 검증 (테스트 97개 통과)
- 집행내역서 xlsx, 검증 리포트 HTML, 잔액 반납 기안 초안
- CLI (오류 시 종료코드 1) + JSON 출력
- 임계값 YAML 오버라이드

**안 되는 것 — 다음 작업 후보**

| 우선순위 | 할 일 | 왜 |
|---|---|---|
| 높음 | **HWP/HWPX·PDF 파서** | 지금은 텍스트 단계부터 시작. 실무 서류를 바로 못 던짐 |
| 높음 | **실제 견적서 20건으로 정확도 측정** | 내장 샘플은 너무 깨끗함. 팩스 견적서 기준으로 재보정 필요 |
| 중간 | **국세청 사업자등록 진위확인 연동** | R14가 체크섬만 봄. 폐업·휴업 업체는 못 잡음. `PublicDataReader` 참고 |
| 중간 | **R17 증빙 체크리스트 룰** | 금액 구간별 필수 서류 유무 판정 |
| 낮음 | **HWP 집행내역서 출력** | 지금은 xlsx만. 교육청 제출 서식이 한글이면 필요 |
| 낮음 | PyPI 배포 | `jeongsan` 이름은 아직 비어 있음 (확인 완료) |

---

## 알려진 함정

**추정가격은 부가세를 뺀 금액입니다.** 여기서 실수가 제일 많습니다. 부가세 포함
2,150만원 계약은 추정가격 1,954만원이라 1인 견적이 가능합니다.

**기집행 내역(`prior`)이 비면 잔액이 실제보다 많아 보입니다.** 사업 마스터 JSON에
반드시 채우십시오. 분할수의 판정(R15)도 이 데이터로 돕니다.

**`--line` 값은 사업 JSON의 `lines[].name` 과 정확히 같아야 합니다.**
가운뎃점(·)과 공백까지 일치해야 R07이 제대로 돕니다.

**R01과 R02는 따로 봐야 합니다.** 견적서에 적힌 금액끼리는 맞는데 개별 곱셈만
틀린 경우가 실제로 있습니다. R02만 보고 "합계가 맞으니 괜찮다"고 넘어가면 놓칩니다.
내장 샘플 ②가 정확히 그 케이스입니다.

**계약 기준 금액은 개정됩니다.** 연 1회 소속 교육청 집행지침으로 확인하고
`skills/jeongsan/references/rules.yaml` 을 갱신하십시오. 코드는 손댈 필요 없습니다.

---

## 배포 전 체크리스트

공개하거나 다른 학교에 넘기기 전에:

- [ ] `marketplace.json` 의 `owner`, 저장소 URL을 실제 값으로
- [ ] `LICENSE` 의 저작권자 이름
- [ ] README에 **"판정은 참고용이며 최종 책임은 담당자에게 있다"** 는 면책 명시
- [ ] 계약 기준 금액이 교육청·연도마다 다르다는 안내
- [ ] `tests/fixtures/` 에 실제 학교·업체 정보가 안 들어갔는지 확인
      (현재 값은 전부 가공 데이터. 사업자등록번호도 체크섬만 맞춘 임의 번호)

---

## 참고

- 설계 배경: `skills/jeongsan/references/rules.md` (룰별 근거와 예외)
- 작업 규칙: `CLAUDE.md` (룰 추가 방법, 하지 말 것)
- 라이브러리 사용법: `README.md`
